/**
 * Public surface for the Resources content library (`/resources`).
 */
export type {
  Resource,
  ResourceSection,
  ResourceFaq,
  ResourceSource,
  ResourceCategory,
} from './types'
export {
  resources,
  getResource,
  getAllResources,
  CATEGORY_LABELS,
} from './resources'
