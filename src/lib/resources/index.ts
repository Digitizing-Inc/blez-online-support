/**
 * Public surface for the Resources content library (`/resources`).
 */
export type {
  Resource,
  ResourceSection,
  ResourceFaq,
  ResourceCategory,
} from './types'
export {
  resources,
  getResource,
  getAllResources,
  CATEGORY_LABELS,
} from './resources'
